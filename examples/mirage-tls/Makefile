# Generated by mirage configure -t macos (2019-03-16 19:41:47-00:00).

-include Makefile.user

OPAM = opam
DEPEXT ?= $(OPAM) pin add -k path --no-action --yes mirage-unikernel-http2af_unikernel-macosx . &&\
	$(OPAM) depext --yes --update mirage-unikernel-http2af_unikernel-macosx ;\
	$(OPAM) pin remove --no-action mirage-unikernel-http2af_unikernel-macosx

.PHONY: all depend depends clean build
all:: build

depend depends::
	$(DEPEXT)
	$(OPAM) install -y --deps-only .

build::
	mirage build

clean::
	mirage clean

